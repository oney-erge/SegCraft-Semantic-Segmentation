[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "segcraft"
version = "0.1.5"
description = "Config-driven semantic segmentation framework"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE.md"]
authors = [{ name = "oney-erge" }]
keywords = ["semantic-segmentation", "computer-vision", "pytorch", "video", "segmentation"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
  "Topic :: Scientific/Engineering :: Image Processing",
]
dependencies = [
  "PyYAML>=6.0",
]

[project.urls]
Homepage = "https://github.com/oney-erge/SegCraft-Semantic-Segmentation"
Repository = "https://github.com/oney-erge/SegCraft-Semantic-Segmentation"
Issues = "https://github.com/oney-erge/SegCraft-Semantic-Segmentation/issues"
Changelog = "https://github.com/oney-erge/SegCraft-Semantic-Segmentation/blob/master/CHANGELOG.md"

[project.optional-dependencies]
torch = [
  "numpy>=1.26,<2.0",
  "pillow>=12.3,<13",
  "torch>=2.2,<3",
  "torchvision>=0.17,<1",
]
smp = [
  "segmentation-models-pytorch>=0.3.3,<0.6",
]
transformers = [
  "transformers>=5.5,<6",
]
video = [
  "opencv-python>=4.8,<4.12",
  "yt-dlp>=2024.1",
]
app = [
  "fastapi>=0.115,<1",
  "python-multipart>=0.0.9,<0.1",
  "uvicorn[standard]>=0.30,<1",
]
web = [
  "numpy>=1.26,<2.0",
  "pillow>=12.3,<13",
  "torch>=2.2,<3",
  "torchvision>=0.17,<1",
  "transformers>=5.5,<6",
  "opencv-python>=4.8,<4.12",
  "yt-dlp>=2024.1",
  "fastapi>=0.115,<1",
  "python-multipart>=0.0.9,<0.1",
  "uvicorn[standard]>=0.30,<1",
]
dev = [
  "pytest>=9.0.3,<10",
  "build>=1.2,<2",
  "twine>=5.0,<7",
  "packaging>=23.2,<27",
  "docutils>=0.18.1,<0.24",
  "rich>=13,<16",
]

[project.scripts]
segcraft = "segcraft.cli.main:main"
segcraft-web = "segcraft.webapp:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"segcraft.templates" = ["*.yaml"]
"segcraft.templates.presets" = ["*.yaml"]
